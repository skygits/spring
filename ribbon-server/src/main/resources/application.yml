spring:
  application:
    name: ribbon-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:admin@localhost:8080/eureka/
  instance:
    hostname: localhost
    instance-id: http://localhost:8088
server:
  port: 8088